# TRACE-Net H30 Phase 4.5.8 — First-38 Discovery Contract

The interrupted full-180 exposed five repeatable issues through q038: descriptive tokens were mistaken for candidate identifiers; partial `item number` wording was routed as an IPL row; validated planner adoption was compared only with deterministic routing; `physical_description` lacked a topic mapping; and descriptive discovery routes did not receive visible structured follow-ups.

This patch fixes those shared contracts while preserving genuine `item 14` table lookups, direct-evidence gates, candidate-only wording, executor-owned tunnel selection, and all read-only safety rules. It also recognizes a small allow-list of alphabetic aviation-family prefixes such as `NAS` and expands common nomenclature terms through q044.
