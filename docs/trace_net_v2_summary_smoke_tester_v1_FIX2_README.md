# TRACE-Net V2 summary smoke tester v1 fix 2

Fixes the tester compatibility issue by allowing prompt prose with required field names to pass, and by overwriting blank compatibility defaults in normalized smoke cards rather than using setdefault only.
