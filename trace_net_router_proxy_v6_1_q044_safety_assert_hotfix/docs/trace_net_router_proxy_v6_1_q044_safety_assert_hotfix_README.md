# TRACE-Net router proxy v6.1 q044 safety assert hotfix

Test-only hotfix. It updates the q044 regression test to read safety counters from either the top-level response shape or the nested `safety_contract` returned by the internal fast-clarification path.

No router logic is changed.
