# Page image recognition quality counts fix

Replaces `tiff/page_image_recognition_quality.py` with a robust parser for the current image-recognition audit JSON shape and the older unit-test shapes.

It restores all public functions expected by tests and scripts.
