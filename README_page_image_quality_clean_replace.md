# Page image-recognition quality clean replacement

Replaces `tiff/page_image_recognition_quality.py` with a clean, stable parser for the current page image-recognition audit JSON and older test shapes. It keeps all public function names expected by scripts/tests.
